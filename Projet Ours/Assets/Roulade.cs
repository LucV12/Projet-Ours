using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Roulade : MonoBehaviour
{

   /* IEnumerator Roll()   //C'est le code de la Roulade.
    {

        if (Input.GetKey(KeyCode.LeftArrow))
        {
            bear.velocity = Vector2.left * dashSpeed;
            canMove = false;               //Si on laisse la possibilité de se déplacer pendant le dash, le déplacement va annuler cette dernière.
        }
        else if (Input.GetKey(KeyCode.RightArrow))
        {
            bear.velocity = Vector2.right * dashSpeed;
            canMove = false;
        }
        else if (Input.GetKey(KeyCode.UpArrow))
        {
            bear.velocity = Vector2.up * dashSpeed;
            canMove = false;
        }
        else if (Input.GetKey(KeyCode.DownArrow))
        {
            bear.velocity = Vector2.down * dashSpeed;
            canMove = false;
        }
        yield return new WaitForSeconds(dashTime);
        bear.velocity = Vector2.zero;
        canMove = true;
    }*/


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
